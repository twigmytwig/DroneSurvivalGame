use bevy::prelude::*;

#[derive(Component)]
pub struct HitBox{
    pub height: f32,
    pub width: f32,
}

//Idea is to determine if this hitbox collides with another hitbox?
pub fn handle_collision(){

}